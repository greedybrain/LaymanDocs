class PostFetch {

     static setUpOptions() {

     }

     static getUrlAndPasteData(urlInput, pasteInput) {
          const data = {
               url: urlInput,
               pasteInfo: pasteInput
          }
          const options = {
               method:
          }
     }
}